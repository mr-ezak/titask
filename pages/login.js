import styled from "styled-components"

const Login = () => (
  <div>
    <LoginBox />
  </div>
)

export default Login

const LoginBox = styled.div`
  width: 300px;
  height: 300px;
  background: #ccc;
  margin: 200px auto;
`
